'use client';

import FlexBasic_default from "../../../Flex/FlexBasic.mjs";
import { useActionBarStyles } from "../style.mjs";
import { jsx, jsxs } from "react/jsx-runtime";
import { useResponsive } from "antd-style";

//#region src/chat/ChatInputArea/components/ChatInputActionBar.tsx
const ChatInputActionBar = ({ ref, padding = "0 16px", leftAddons, rightAddons, ...rest }) => {
	const { mobile } = useResponsive();
	const { styles } = useActionBarStyles();
	return /* @__PURE__ */ jsxs(FlexBasic_default, {
		align: "center",
		className: styles.root,
		flex: "none",
		horizontal: true,
		justify: "space-between",
		padding,
		ref,
		...rest,
		children: [/* @__PURE__ */ jsx(FlexBasic_default, {
			align: "center",
			className: styles.left,
			flex: 1,
			gap: mobile ? 0 : 4,
			horizontal: true,
			children: leftAddons
		}), /* @__PURE__ */ jsx(FlexBasic_default, {
			align: "center",
			className: styles.right,
			flex: 0,
			gap: mobile ? 0 : 4,
			horizontal: true,
			justify: "flex-end",
			children: rightAddons
		})]
	});
};
ChatInputActionBar.displayName = "ChatInputActionBar";
var ChatInputActionBar_default = ChatInputActionBar;

//#endregion
export { ChatInputActionBar_default as default };
//# sourceMappingURL=ChatInputActionBar.mjs.map