import { createStyles } from "antd-style";
import { rgba } from "polished";

//#region src/awesome/GradientButton/style.ts
const useStyles = createStyles(({ isDarkMode, cx: cx$1, css: css$1, token, stylish }, size) => {
	let radius;
	switch (size) {
		case "large":
			radius = token.borderRadiusLG;
			break;
		case "middle":
			radius = token.borderRadius;
			break;
		case "small":
			radius = token.borderRadiusSM;
			break;
		default:
			radius = token.borderRadius;
			break;
	}
	return {
		button: css$1`
        position: relative;
        z-index: 1;
        border: none;
        border-radius: ${radius}px !important;

        &::before {
          ${stylish.gradientAnimation}
          content: '';

          position: absolute;
          z-index: -2;
          inset: 0;

          border-radius: ${radius}px;
        }

        &::after {
          content: '';

          position: absolute;
          z-index: -1;
          inset-block-start: 1px;
          inset-inline-start: 1px;

          width: calc(100% - 2px);
          height: calc(100% - 2px);

          background: ${isDarkMode ? token.colorBgLayout : token.colorBgContainer};
          border-radius: ${radius - 1}px;
        }

        &:hover {
          &::after {
            background: ${rgba(isDarkMode ? token.colorBgLayout : token.colorBgContainer, isDarkMode ? .9 : .95)};
          }
        }

        &:active {
          &::after {
            background: ${rgba(isDarkMode ? token.colorBgLayout : token.colorBgContainer, isDarkMode ? .85 : .9)};
          }
        }
      `,
		glow: cx$1(stylish.gradientAnimation, css$1`
          position: absolute;
          z-index: -2;
          inset-block-start: 0;
          inset-inline-start: 0;

          width: 100%;
          height: 100%;

          opacity: 0.5;
          filter: blur(0.5em);
          border-radius: inherit;
        `)
	};
});

//#endregion
export { useStyles };
//# sourceMappingURL=style.mjs.map