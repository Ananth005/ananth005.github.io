import { createStyles } from "antd-style";

//#region src/awesome/Spotlight/style.ts
const useStyles = createStyles(({ css: css$1, token, isDarkMode }, { offset, outside, size }) => {
	const spotlightX = (offset?.x ?? 0) + "px";
	const spotlightY = (offset?.y ?? 0) + "px";
	return css$1`
      pointer-events: none;

      position: absolute;
      z-index: 1;
      inset: 0;

      opacity: ${outside ? "0" : ".1"};
      background: radial-gradient(
        ${size + "px"} circle at ${spotlightX} ${spotlightY},
        ${isDarkMode ? token.colorText : "#fff"},
        ${isDarkMode ? "transparent" : token.colorTextQuaternary}
      );
      border-radius: inherit;

      transition: all 0.2s;
    `;
});

//#endregion
export { useStyles };
//# sourceMappingURL=style.mjs.map