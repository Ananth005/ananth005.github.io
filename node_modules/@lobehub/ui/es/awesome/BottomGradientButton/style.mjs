import { createStyles } from "antd-style";

//#region src/awesome/BottomGradientButton/style.ts
const useStyles = createStyles(({ cx: cx$1, stylish, css: css$1, token }) => cx$1(stylish.resetLinkColor, css$1`
      overflow: hidden;
      font-weight: bold;
      color: ${token.colorTextSecondary};
      transition: all 0.2s ease-in-out;

      &::before {
        content: '';

        position: absolute;
        inset-block-end: 0;

        display: block;

        width: 50%;
        height: 1px;

        opacity: 0;
        background-image: linear-gradient(to right, transparent, ${token.gold}, transparent);

        transition: all 0.2s ease-in-out;
      }

      &:hover {
        &::before {
          opacity: 1;
        }
      }
    `));

//#endregion
export { useStyles };
//# sourceMappingURL=style.mjs.map