import { createStyles } from "antd-style";

//#region src/Tag/styles.ts
const useStyles = createStyles(({ stylish, prefixCls, css: css$1, token }) => ({
	borderless: stylish.variantBorderlessWithoutHover,
	filled: stylish.variantFilledWithoutHover,
	large: css$1`
    &.${prefixCls}-tag {
      height: 28px;
      padding-inline: 12px;
      border-radius: 6px;
    }
  `,
	outlined: stylish.variantOutlinedWithoutHover,
	root: css$1`
    color: ${token.colorTextSecondary};
    &.${prefixCls}-tag {
      user-select: none;

      display: flex;
      gap: 0.4em;
      align-items: center;
      justify-content: center;

      width: fit-content;
      height: 22px;
      margin: 0;

      line-height: 1.2;

      border-radius: 3px;

      span {
        margin: 0;
      }
    }
  `,
	small: css$1`
    &.${prefixCls}-tag {
      height: 20px;
      padding-inline: 4px;
    }
  `
}));

//#endregion
export { useStyles };
//# sourceMappingURL=styles.mjs.map